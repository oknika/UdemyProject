<Window x:Class="MyMemo.MnTools.WinLogin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyMemo.MnTools"
        mc:Ignorable="d"
        Title="Login" Width="400" Height="Auto"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize" Icon="/Resources/Img/Menus/9026122_user_switch_icon.png">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- GroupBox: User Info -->
        <GroupBox Header="User info" FontWeight="Bold" BorderThickness="2" Grid.Row="0">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- User Image -->
                <Border Height="144" Width="150" BorderBrush="#FF171111" BorderThickness="1" CornerRadius="5" HorizontalAlignment="Center">
                    <Image x:Name="imgUser"/>
                </Border>

                <!-- Form Fields -->
                <Grid Grid.Row="1" Margin="0,20,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Label Content="Username" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,5"/>
                    <ComboBox x:Name="cmbUser" Grid.Row="0" Grid.Column="1" Width="204" SelectionChanged="cmbUser_SelectionChanged" Margin="0,0,0,5">
                        <ComboBoxItem Content="User1"/>
                        <ComboBoxItem Content="User2"/>
                        <ComboBoxItem Content="User3"/>
                    </ComboBox>

                    <Label Content="Password" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <PasswordBox x:Name="txtPwd" Grid.Row="1" Grid.Column="1" Width="204" VerticalContentAlignment="Center"/>
                </Grid>
            </Grid>
        </GroupBox>

        <!-- Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="btnLogin" Style="{DynamicResource btnGeneral}" Click="btnLogin_Click" Margin="0,0,10,0">
                <StackPanel Orientation="Horizontal">
                    <Image Source="/Resources/Img/Menus/48697_login_icon.png" Width="15" Height="15"/>
                    <Label Content="Login" Style="{DynamicResource lblMnBtns_style}" Padding="3,0,0,0"/>
                </StackPanel>
            </Button>
            <Button x:Name="btnCancel" Style="{DynamicResource btnGeneral}" Click="btnCancel_Click">
                <StackPanel Orientation="Horizontal">
                    <Image Source="/Resources/Img/Menus/9004828_cross_delete_remove_cancel_icon.png" Width="15" Height="15"/>
                    <Label Content="Cancel" Style="{DynamicResource lblMnBtns_style}" Padding="3,0,0,0"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</Window>
